usePlugin 'java'

dependencies {
    compile fileTree(dir: 'lib/deploy', includes: ['*.jar'])
    runtime fileTree(dir: 'lib/develop', includes: ['*.jar'], excludes: ['*-src.jar'])
}

test {
    options.systemProperties['com.objogate.wl.keyboard'] = 'US'
}